import { defineConfig } from 'vite';

export default defineConfig({
  // WAŻNE: Wpisz nazwę swojego repozytorium GitHub zamiast tekstu poniżej,
  // na przykład: base: '/supabase-blog-project/'
  base: '/nazwa-twojego-repozytorium/', 
});